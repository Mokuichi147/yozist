-- コミットごとの論理サイズ（提示サイズ = 一覧やダウンロードで見えるサイズ）。
-- バージョン別ストレージ集計（最新版 / 過去バージョン維持の容量）に使う。
-- blob はオンディスクでは zstd 圧縮されており実体サイズと論理サイズが大きく
-- 異なるため、ファイル一覧の size と一致する論理サイズをコミット単位で保持する。
-- 既存行は 0（不明）。新規コミットから記録する。
ALTER TABLE commits ADD COLUMN size INTEGER NOT NULL DEFAULT 0;
